VITE_ROOMS_SERVER_URL=http://localhost:4000

# Aptabase Analytics (https://aptabase.com)
# App key from the Aptabase dashboard (Instructions menu). Leave empty to disable telemetry entirely.
VITE_APTABASE_APP_KEY=
# Optional: point to a self-hosted Aptabase instance (e.g. https://aptabase.example.com). Leave empty to use Aptabase Cloud.
VITE_APTABASE_HOST=
